"""Legacy connection-model module.

Connection API models live in app.api.schemas.connection.  The uploaded source
contained terminal transcript text in this file, which is invalid Python; this
placeholder keeps fresh container builds deterministic without changing the
active connection API contract.
"""
